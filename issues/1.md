---\n**Title:** Dependency conflict: fastapi 0.115.12 requires starlette <0.47.0 but requirements pin starlette==0.49.1 — CI Docker build failing\n\n**Description:**\nThe GitHub Actions job that builds the Docker image is encountering a dependency conflict between FastAPI and Starlette due to mismatched version constraints in `requirements.txt`. Here's the summary:\n\n**Problem**\n- **FastAPI version:** 0.115.12  \n- **Starlette version:** 0.49.1\n- **Issue:** FastAPI 0.115.12 depends on Starlette version `>=0.40.0` and `<0.47.0`. However, the current configuration specifies `starlette==0.49.1`, which is out of the allowed range.\n\n**Build Failure**\nThe build fails at the following step in the Dockerfile:\n``\nRUN pip install -r requirements.txt\n``\nwith the error:\n``\nERROR: Cannot install -r requirements.txt (line 5) and starlette==0.49.1 because these package versions have conflicting dependencies.\n``\n**Fix Recommendation**\n1. Modify `requirements.txt` with Starlette in a compatible range:\n```plaintext\nstarlette>=0.40.0,<0.47.0\n```\nor remove the version pin on Starlette entirely.\n   ```plaintext\nstarlette\n   ```\n2. Run tests and restart the CI workflow to validate.\n\n### Logs\nRefer to the failing [Actions Job Run](https://github.com/MJHDrummond/air-quality-prediction-webapp/actions/runs/21094046422/job/60669118610).\n\n**Relevant Files**\n1. [requirements.txt@c463e49e](https://github.com/MJHDrummond/air-quality-prediction-webapp/blob/c463e49e8713ce36143d2cb326254f5326bdb9ed/requirements.txt#L5)\n2. [Dockerfile@c463e49e](https://github.com/MJHDrummond/air-quality-prediction-webapp/blob/c463e49e8713ce36143d2cb326254f5326bdb9ed/Dockerfile#L8)